{
  "name": "toddler-math",
  "version": "1.0.0",
  "private": true,
  "description": "Add Along! — an offline, single-file toddler addition game.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "1.56.1"
  }
}
